# Season

The season simulator — section 6 of the Fantasy Hockey AI build plan, and the evaluation harness
section 16 needs. Four modelling layers were built before it, each validated against its own job:

```
ModelFeatures/   447 features, leakage asserted        "is this feature knowable at the lock?"
Projections/     a mean per player-game, and per ROS   "how close is this projection?"
Simulation/      a distribution, calibrated            "is this spread right?"
Season/          a won week                            "does using any of it beat not using it?"
```

Nothing before this folder asks the last question. That is the gap this closes.

**It opens no database.** Like `Projections/` and `Simulation/`, everything arrives by path. It is
the first folder that imports *code* from a sibling — `Simulation/`'s sampler, copula and scoring —
because drawing a night is a computation rather than a file. That dependency lives in `simlayer.py`
and nowhere else, and it is narrower than it looks: `Simulation/simulate.py` and `correlations.py`
**cannot** be imported at all, because `Season/paths.py` shadows `Simulation/paths.py` under the
flat-module convention both folders use. The two fitted JSONs they would have supplied are read by
path instead, which is how every other layer here consumes the one below it.

**It measures; `Decisions/` chooses.** Every policy the ladder seats -- the four rungs, the lineup
solver, the draft rule, rung 3's box-score estimator -- lives in the sibling `Decisions/` folder so
a live runner can use the same code a backtest scored. It is reached through `decisionlayer.py`,
which refuses to start if a module name in `Decisions/` also exists here or in `Simulation/`.

```
paths.py      where every input lives
league.py     the format as data: slots, bench, IR, the move cap, the playoff shape
schedule.py   game days and Mon-Sun matchup weeks, derived from the schedule, never assumed
inputs.py     the loaders, and the provenance assertion that makes the measurement honest
state.py      rosters, IR, the free-agent pool, waivers, the weekly move counter
view.py       SlateView -- the only thing a manager sees
draftroom.py  the draft as an event: seat order, the snake; the pick itself is Decisions/draft.py
simlayer.py   the bridge to Simulation/, and the module-name collision it works around
decisionlayer.py  the bridge to Decisions/ (managers, slots, draft, estimators, valuation, adddrop)
engine.py     the day loop: lock, resolve, accumulate by week
ladder.py     CLI                                          -> reports/ladder_<season>.json
report.py     the results as prose                         -> docs/ladder-<config>.md
compare.py    two formats against each other, paired
verify.py     the eleven checks that have to pass before a number means anything
```

## Running it

```bash
pip install -r requirements.txt

# once, from ModelFeatures/ -- the only steps that touch the database
python build_goalie_starts.py --season 2023-24 --season 2024-25 --season 2025-26
python build_fantasy_positions.py --platform Yahoo --season 2026-27 --adp

# once, from Projections/ -- rung 4 reads the first, rung 5 the second (holdout build only)
python goalie_starts.py --train --save --predict --season 2025-26
python ros_train.py --train 2023-24 2024-25 --test 2025-26 --horizon season --predictions-out

python ladder.py --season 2025-26 --weights points-league --weights banger-league \
    --replications 8 --decision-sims 120
python ladder.py --league league-12team-simple --replications 8 \
    --weights points-league --weights banger-league
python ladder.py --rung 2 --rung 4 --rung 5 --rung 6 --replications 8 --tag adddrop     --weights points-league --weights banger-league      # section 9: add/drop against hold
python ladder.py ... --horizon-weeks season --margin 0 --rate-source per_game --tag sens-x
python compare.py --a league-12team-simple --b league   # what a format change does
python verify.py         # the eleven checks, from provenance to dark nights and opening rates
```

## What the ladder measured

`docs/ladder-league.md` and `docs/ladder-league-12team-simple.md` carry the tables, generated by
`report.py`. Eight seat rotations of the real 2025-26 schedule; in the 14-team target format that is
four clones a rung, so each rung is a mean over 28 team-seasons. Outcomes are the actual stat lines,
so there is no Monte Carlo noise in them; the spread below is across seats.

**Every number here is from after two bugs were fixed (2026-09-22), and both reversed the verdict
this section used to carry.** A rostered player whose club was idle tonight had no team in the
manager's view, and a player whose club had not opened yet had no rate -- either way he priced at
zero and was the first man dropped. Every transacting rung spent the season dropping players for a
dark night. The earlier "spending the seven-move budget is actively harmful" finding was that bug and
nothing else. `verify.py` now guards both (`dark nights`, `opening rates`), and a player the
projections have not reached yet falls back to last season's rate per team game
(`Decisions/draft.prior_season_team_game_rate`) rather than to zero.

| 14-team, points-league | points / week | ±SE | win rate | decision eff. | wasted slot-nights | moves |
|---|---|---|---|---|---|---|
| 1 — autodraft, never look again | 144.50 | 2.22 | 0.254 | 0.826 | 1392 | 0 |
| 2 — start every healthy player | 168.62 | 1.92 | 0.467 | 0.963 | 74 | 0 |
| 3 — capped streamer, box-score rate | 186.80 | 3.02 | 0.610 | 0.951 | 116 | 175 |
| 4 — full system | **191.27** | 2.18 | **0.669** | 0.952 | 77 | 182 |

Paired inside each league instance, which is the only fair way to difference two rungs that shared a
schedule and a free-agent pool:

| 14-team | points-league | banger-league |
|---|---|---|
| rung 2 − rung 1 — attention | **+23.78 ± 1.27** | **+57.61 ± 4.68** |
| rung 3 − rung 2 — naive streaming | **+18.37 ± 2.16** | **+37.63 ± 4.75** |
| rung 4 − rung 2 — the full system | **+22.68 ± 2.58** | **+36.11 ± 5.60** |
| rung 4 − rung 3 — *the modelling stack* | **+4.31 ± 2.05** | −1.52 ± 2.82 |

### Section 16's question, answered

> *Full system. Projections, Monte Carlo, orchestrator. If it doesn't clear rung 3 decisively, the
> modeling investment isn't paying for itself.*

**Only just, and only in the target format.** +4.31 ± 2.05 points a week under points-league scoring
-- about two standard errors -- and −1.52 ± 2.82 in a banger league. In the 12-team format it is
+0.79 ± 1.26 and −7.29 ± 3.24. By this section's own standard the modelling stack, *as rung 4 uses
it*, does not clear rung 3 decisively; section 9's add/drop rule (below) is what changes that --
the same lineup with a better transaction rule scores 197.7 a week in the target format, against
rung 3's 186.80 in the ladder's field (different fields, so not paired).

What does clear the noise, everywhere:

- **Setting a lineup daily is worth +24 points a week** in the target format and +58 in a banger
  league. It costs no transactions and no models.
- **Spending the seven-move budget is worth almost as much again**: +18 a week for a streamer with
  no model at all, +38 under banger scoring. Once a player on an idle team keeps his games, filling
  empty slot-nights from the wire is the second-largest effect in the ladder.
- Rung 1 is a floor by a wide margin, as section 16 predicted.

### What rung 4 does and does not do

It is better than rung 3 at what it was built for: **77** wasted slot-nights against rung 3's
**116** -- the fitted P(plays) working as designed -- and a lower spread across seats. What it does
not do is transact well. Its 182 moves a season gain **+3 rest-of-season points each, with a 52% hit
rate**: it churns the wire with the whole budget, and better rankings of marginal free agents are not
worth much. Every acquisition trades talent for games, because **the free-agent pool is the leftovers
of a fourteen-team draft**.

The lineup half has little to work with either: only about half the NHL plays on a given night, so a
roster has roughly **seven** startable players against twelve to fourteen active slots, and decision
efficiency is already 0.95–0.97 against the hindsight-optimal lineup from the same roster. With
transactions switched off (rung 6 below), rung 4's lineup is level with rung 2's in the target
format: **+1.8 ± 2.3**.

## Section 9: the add/drop rule

`Decisions/adddrop.py`, seated as rung 5: rung 4's lineup with its transactions replaced. It prices
a swap **on the roster** -- `Decisions/valuation.py` re-solves the lineup on every night either
player's team plays, with and without the swap -- values both sides with the rest-of-season rate
over `H` weeks, and moves only when the gain clears `m` standard deviations of itself. A rostered
player with no rate yet is never a drop candidate: unknown is not zero. Rung 6 is the same manager at
`m = ∞` -- it never moves -- and is the comparison section 9 asks for.

The shipped default is **H = 3, m = 1, rest-of-season rate, no waiver claims**
(`docs/ladder-league_adddrop.md`, `docs/ladder-league-12team-simple_adddrop.md`, 8 rotations):

| format | rung 5 − hold | rung 5 − rung 2 | rung 5 − rung 4 | moves |
|---|---|---|---|---|
| 14-team, points | **+27.0 ± 1.8** | **+28.7 ± 2.7** | **+13.8 ± 1.4** | 23 |
| 14-team, banger | **+32.6 ± 4.9** | **+45.8 ± 2.1** | **+6.2 ± 1.7** | 25 |
| 12-team, points | **+20.0 ± 2.7** | **+21.0 ± 1.7** | **+9.0 ± 1.8** | 17 |
| 12-team, banger | **+38.7 ± 1.8** | **+34.1 ± 3.8** | **+9.7 ± 1.5** | 18 |

**It clears every bar in all four combinations**: hold, a manager who never touches the wire, and
rung 4's own transaction rule, which it beats by 6 to 14 points a week on a ninth of the moves.
Graded over the rest of the season, rung 5's drops gained **+23 points each with a 71% hit rate**,
against rung 4's +3 and 52% (one rotation, 14-team, points-league, H = 1).

**Sensitivity, not tuning.** 2025-26 is the only clean holdout, so the parameters were only read off
it coarsely (`docs/ladder-league_sens-*.md`, 14-team, 4 rotations, rung 5 − rung 6):

| setting | moves | points-league | banger-league |
|---|---|---|---|
| H = 1, m = 1 (8 rotations) | 30 | +23.5 ± 1.0 | +27.0 ± 4.5 |
| **H = 3** | 23 | +25.7 ± 3.1 | +33.2 ± 2.2 |
| H = rest of season | 27 | +26.4 ± 3.6 | +37.9 ± 4.1 |
| m = 0 | 180 | +26.6 ± 4.4 | +35.9 ± 5.7 |
| m = 2 | 2 | +3.7 ± 2.4 | −1.0 ± 2.7 |
| per-game carried rate instead of rest-of-season | 61 | **+29.8 ± 1.5** | **+44.9 ± 3.0** |

The m, per-game and horizon rows vary one setting around H = 1, m = 1. Two readings to carry into
section 11, which tunes these on 2024-25: **a longer horizon helps**, and **the per-game carried rate
beat the rest-of-season projection** as the rule's valuation -- by about 6 points a week under points
scoring and 18 under banger. That is a result against the rest-of-season model as an input to this
decision, and it should be checked on a second season before anything is built on it.

## Formats

Both formats at eight seat rotations, both scoresets, identical everything else:

```
A  12 teams   2C 2LW 2RW 4D 2G          12 active, 4 bench, 22 weeks   rosters/league-12team-simple.json
B  14 teams   2C 2LW 2RW 1F 4D 1F/D 2G  14 active, 4 bench, 26 weeks   rosters/league.json
```

`F` accepts any forward and `F/D` any skater, so B has two **composite** slots. Points per week are
not comparable across formats — 14 active slots outscore 12 by construction — so what follows is the
gap *between rungs*, paired inside each league instance.

| points-league | A: 12-team simple | B: 14-team composite |
|---|---|---|
| attention (rung 2 − 1) | +25.37 ± 1.93 **clear** | +23.78 ± 1.27 **clear** |
| naive streaming (3 − 2) | +18.07 ± 3.19 **clear** | +18.37 ± 2.16 **clear** |
| full system vs not transacting (4 − 2) | +18.86 ± 2.86 **clear** | +22.68 ± 2.58 **clear** |
| **the modelling stack (4 − 3)** | +0.79 ± 1.26 *noise* | +4.31 ± 2.05 *borderline* |

Under banger scoring the modelling-stack gap is −7.29 ± 3.24 in A and −1.52 ± 2.82 in B. The format
still moves the verdict the same way it did before the fixes -- composite slots and a deeper league
favour rung 4 -- but attention and streaming now dominate in both.

### What this says about measuring any of it

The verdict on the same code, the same season and the same models has reversed twice: once on a
roster-format change, and once on a bug that priced idle and not-yet-seen players at zero. The second
is the more important lesson. It produced a plausible, publishable number -- "transacting loses" --
for the whole life of the ladder, and it surfaced only when predicted and realized gain were logged
per move and dropped players were graded over the rest of the season. **Move-level accounting caught
it; a season-level table never would have.** A result here is a statement about a *format*, which is
why the config file is data and why report paths are keyed by config.

## The goalie model

`Projections/goalie_starts.py`, the one goalie output that survived measurement. Held out of
2025-26 entirely, since the harness consumes it:

| | holdout AUC |
|---|---|
| **the model** | **0.876** |
| season start share | 0.721 |
| dressed share | 0.646 |
| "he started the last one" | 0.518 |

Well calibrated too — a predicted 0.030 starts 2.9% of the time, a predicted 0.943 starts 94.2%.
`start_share_edge` and `start_share_rank` take 43% of the fitted gain between them, which is the
design point: **a start is a contest inside a team-game, not a property of a goalie**, so the
informative features are relative ones. Predictions are normalized within team-game because
exactly one goalie starts.

Nothing here predicts how well a goalie will play, and that is deliberate: per-start points
measured at R² −0.8% and save percentage carries at r = +0.026. The line is the league average,
and it is **computed from the scoring file rather than hard-coded** — under points-league a start
is worth 4.39 with a spread of 4.03, under banger-league 3.59 and 3.31, and under a
goals-and-assists-only format −3.02, where a goalie is a liability rather than an asset.

## What is honest about these numbers, and what is not

**The projections are out of sample.** `inputs.py` reads `predictions_A.parquet`, the holdout
build's scored season, and refuses `lambdas_2025-26_A.parquet` — which is produced by whatever
boosters sit in `Projections/models/`, currently the deployment build trained on all three seasons
and therefore in-sample on the season being replayed. Handing that to rung 4 and a box-score rate
to rung 3 would compare a manager who has seen the results against one who has not.

**A manager cannot see outcomes.** Not by convention: `view.SlateView` raises on any column
matching `target_*`, `label_*` or the goalie line, so adding a field to the view cannot quietly
smuggle one through.

**Injuries are persistent and exogenous.** `injured_at_lockout` is the real season's history, so a
player out for six weeks is out for six weeks and IR is a real lever. Without it availability
would be a memoryless nightly coin flip and injury replacement — most of real streaming — would
not exist.

**Three known approximations, all measured:**

- Short-handed points are set to zero, because `predictions_A.parquet` predates that model. They
  are 0.38% of skater scoring (496 of 129,177 points on 396 of 46,654 played rows) and the
  understatement is identical for every manager. `inputs.py` has the rebuild that removes it.
- Twelve of 1,312 games are outside the simulated season, all of them 7–9 October: variant-A
  features need a previous game, so the candidate universe starts a few days in.
- Positional eligibility is Yahoo's for 2026-27 applied to 2025-26. It only widens which lineups
  are legal, and it widens them for everyone at once. **ADP is not treated this way** — a 2026-27
  draft board encodes 2025-26 results, so the draft uses a 2024-25 points ranking instead.

**Free IR flatters the do-nothing strategy.** Two IR slots, stashing that costs no move, and an
injury layer taken from the real season mean a manager who never transacts barely suffers for
injuries — he parks the injured and carries on. In a league with tighter IR, or none, the
transaction rungs would look better still. The comparison is fair within this
configuration and should not be read past it.

**One season is one season, and eight rotations only average the draft.** Outcomes here are the real
2025-26 lines, so rotating seats varies draft position and field interaction but not what the
players did. A ±3-point standard error is the spread across seats, not a confidence interval on
"would this strategy win next year". Phase 3's sampled outcomes are what would give that.

**Not covered.** Multi-replication with sampled outcomes, trades, and the live lockout snapshot
job. A win rate here is field-relative — all rungs sit in one league and play each other, so
removing a rung moves everyone's — and a single season is a single season. `points_per_week` is
the portable number until phase 3 puts error bars on both.
