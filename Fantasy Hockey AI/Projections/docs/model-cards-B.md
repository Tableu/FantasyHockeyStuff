# Skater projection models -- variant B

Generated by `modelcards.py` from the run's own artefacts. The **deployed** boosters in `models/` are trained on 2023-24 + 2024-25 + 2025-26 (early stop from 2026-03-07), 447 features.

> **The accuracy figures below are not from these boosters.** This is a deployment build with nothing held back, so it produces no metrics. The numbers come from the last scored build -- trained on 2023-24 + 2024-25, scored on 2025-26 -- which shares this architecture and feature set but saw one season less. Treat them as the best available estimate, not a measurement of what is shipping.

These models project **stats**, not fantasy points -- no scoring system is assumed anywhere in this folder. Composite figures below appear only for scoring files that were passed to `evaluate.py --weights`.

## Accuracy against the two naive baselines

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

| target | mean actual | mean predicted | MAE | best baseline MAE | Spearman | CRPS | PIT deviation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `shots` | 1.545 | 1.563 | 1.013 | 1.044 | 0.432 | 0.682 | 0.008 |
| `hits` | 1.131 | 1.120 | 0.857 | 0.879 | 0.530 | 0.553 | 0.004 |
| `blocks` | 0.787 | 0.829 | 0.728 | 0.732 | 0.405 | 0.442 | 0.020 |
| `assists` | 0.290 | 0.282 | 0.398 | 0.399 | 0.269 | 0.214 | 0.005 |
| `goals` | 0.171 | 0.165 | 0.265 | 0.266 | 0.259 | 0.139 | 0.004 |
| `pim` | 0.484 | 0.425 | 0.742 | 0.773 | 0.143 | 0.423 | 0.049 |

## P(plays)

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

AUC 0.9909, Brier 0.0286, log loss 0.0989 against a base rate of 0.754. Reliability by decile of predicted probability:

| predicted bin | n | mean predicted | observed rate |
| --- | --- | --- | --- |
| 0.0-0.1 | 35,865 | 0.0134 | 0.0122 |
| 0.1-0.2 | 2,876 | 0.136 | 0.1047 |
| 0.2-0.3 | 1,517 | 0.2156 | 0.2156 |
| 0.3-0.4 | 1,066 | 0.3452 | 0.2908 |
| 0.4-0.5 | 1,185 | 0.4518 | 0.443 |
| 0.5-0.6 | 1,012 | 0.5182 | 0.5336 |
| 0.6-0.7 | 3,489 | 0.6561 | 0.6294 |
| 0.7-0.8 | 3,521 | 0.7873 | 0.7433 |
| 0.8-0.9 | 4,727 | 0.8571 | 0.8227 |
| 0.9-1.0 | 130,319 | 0.9898 | 0.9885 |

## Ice time

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

| target | mean actual | MAE | RMSE | Spearman |
| --- | --- | --- | --- | --- |
| `toi` | 992s | 108.2s | 143.2s | 0.868 |
| `ev_toi` | 837s | 110.3s | 143.2s | 0.786 |
| `pp_toi` | 85s | 46.2s | 69.8s | 0.775 |

## Under `points-league` scoring

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

Head-to-head weekly points league: balanced, with PP/SH point bonuses.

Weights: `goals` 4, `assists` 2.5, `ppp` 1, `shp` 1.25, `hits` 0.4, `blocks` 0.4, `shots` 0.25, `pim` 0.2.

MAE 1.856 on a mean of 2.77 points per game, Spearman 0.349, top-100 start/sit capture 0.700 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 1.897 |
| last-10 | 1.920 |

## Under `banger-league` scoring

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

Peripheral-heavy: hits, blocks and PIM carry most of the value.

Weights: `goals` 3, `assists` 2, `ppp` 0.5, `shp` 0.5, `hits` 1.5, `blocks` 1.5, `shots` 0.5, `pim` 1.

MAE 2.537 on a mean of 5.28 points per game, Spearman 0.398, top-100 start/sit capture 0.756 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 2.632 |
| last-10 | 2.671 |

## Under `scoring-only` scoring

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

Goals and assists only -- no peripherals. The hardest format to project.

Weights: `goals` 6, `assists` 4, `ppp` 2, `shp` 2.

MAE 2.698 on a mean of 2.40 points per game, Spearman 0.357, top-100 start/sit capture 0.644 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 2.694 |
| last-10 | 2.703 |

## Walk-forward -- the honesty check

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

Refit monthly on an expanding window across the holdout season, which is how the season simulator will actually consume these models. It buys little on accuracy and helps mainly with level -- tracking the season's own rates narrows the categories that drifted (see the bias column) -- so the single fit stays the default.

| target | bias, single -> walk-forward | walk-forward MAE | single-fit MAE | PIT deviation |
| --- | --- | --- | --- | --- |
| `shots` | +1.2% -> +0.5% | 1.012 | 1.013 | 0.007 |
| `hits` | -0.9% -> -0.6% | 0.857 | 0.857 | 0.004 |
| `blocks` | +5.3% -> +4.7% | 0.726 | 0.728 | 0.017 |
| `assists` | -2.6% -> -2.3% | 0.400 | 0.398 | 0.004 |
| `goals` | -3.6% -> -3.9% | 0.265 | 0.265 | 0.004 |
| `pim` | -12.2% -> -9.9% | 0.747 | 0.742 | 0.046 |

## The live-feed bound

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

The same boosters scored on variant A's holdout features. `plays` AUC falls to 0.9705; the category projections barely move. That is the expected shape -- knowing tonight's lineup settles whether a player dresses, not how many shots he takes once he does.

| category | MAE | Spearman |
| --- | --- | --- |
| `shots` | 1.014 | 0.431 |
| `hits` | 0.857 | 0.529 |
| `blocks` | 0.728 | 0.403 |
| `assists` | 0.396 | 0.269 |
| `goals` | 0.263 | 0.259 |
| `pim` | 0.740 | 0.138 |

## Dispersion handed to the Monte Carlo layer

*Trained on 2023-24 + 2024-25, scored on held-out 2025-26 — **not** the deployed boosters (see the note at the top).*

`Var = mu + theta * mu^2`, fit by maximum likelihood on the holdout. The shared game-quality variance is the Gamma multiplier's variance: **0.021**.

`Var(Y) = E[Var(Y|mu)] + Var(mu)`, so the observed column is the sum of the last two, not something the NB column should match on its own.

| category | theta | mean lambda | observed variance | NB conditional variance | Var(lambda) | gap |
| --- | --- | --- | --- | --- | --- | --- |
| `shots` | 0.0632 | 1.563 | 2.1772 | 1.7459 | 0.4504 | -0.0191 |
| `hits` | 0.1095 | 1.1203 | 1.93 | 1.3214 | 0.5815 | 0.027 |
| `blocks` | 0.0904 | 0.8289 | 1.0784 | 0.9112 | 0.2231 | -0.056 |
| `assists` | 0.0001 | 0.2821 | 0.3164 | 0.2821 | 0.0267 | 0.0076 |
| `goals` | 0.0001 | 0.1653 | 0.185 | 0.1653 | 0.0132 | 0.0065 |
| `pim` | 7.5172 | 0.4248 | 1.9995 | 2.1048 | 0.0431 | -0.1483 |

## Per-model detail

*The deployed boosters: trained on 2023-24 + 2024-25 + 2025-26.*

### `plays`

Multiplies every other projection: a projection is worth zero if he is a healthy scratch. Fit on every lockout-knowable candidate, not just players who dressed.

- objective `binary`, fit on 501,990 rows (all)
- 235 trees, 37.5s, early-stop binary_logloss 0.1082, auc 0.9901
- top features by gain: `feat_dressed` 60.3%, `feat_starting_goalie` 12.1%, `games_dressed_lookback` 6.4%, `injured_at_lockout` 4.3%, `feat_pair` 1.4%, `mean_toi_l5` 1.3%

### `toi`

Total ice time in seconds. In-game injury truncation is unforecastable but rare (0.5% of played rows under five minutes), so plain L2 holds.

- objective `regression`, fit on 384,861 rows (played)
- 294 trees, 124.6s, early-stop l2 19832.9282, l1 106.1716
- top features by gain: `mean_toi_l5` 43.6%, `mean_toi_l10` 27.5%, `mean_toi_l20` 13.5%, `mean_toi_std` 6.3%, `feat_line` 1.1%, `toi_l5` 0.7%

### `ev_toi`

Even-strength seconds.

- objective `regression`, fit on 384,861 rows (played)
- 151 trees, 21.9s, early-stop l2 20089.6571, l1 109.2413
- top features by gain: `mean_ev_toi_l20` 32.9%, `mean_ev_toi_l10` 30.3%, `mean_ev_toi_std` 11.5%, `mean_ev_toi_l5` 9.8%, `feat_line` 2.2%, `mean_toi_l5` 2.2%

### `pp_toi`

Power-play seconds: a large zero mass for anyone off both units. Tweedie is the obvious shape for that and is again slightly worse -- measured on the early-stop slice, p=1.4 biases the top PP decile -2.9% against Poisson's -0.8% at the same MAE. Same lesson as PIM: Tweedie over-shrinks the tail.

- objective `poisson`, fit on 384,861 rows (played)
- 242 trees, 29.9s, early-stop poisson -312.3977, l1 45.0121
- top features by gain: `mean_pp_toi_l5` 43.5%, `mean_pp_toi_l10` 26.1%, `mean_pp_toi_l20` 9.2%, `feat_pp` 4.3%, `mean_pp_toi_std` 3.3%, `pp_known` 0.8%

### `shots`

High volume and fairly stable; the sanity check for the whole offset scheme, since every other count offsets on the same predicted ice time.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.7641, fit on 384,861 rows (played)
- 576 trees, 129.3s, early-stop poisson 0.7443, l1 0.9956
- top features by gain: `iff_all_p60_std` 26.7%, `shots_p60_std` 6.1%, `ev_shots_p60_std` 4.6%, `icf_all_p60_std` 4.1%, `prev_shots_p60` 3.6%, `ixg_all_p60_std` 3.5%

### `hits`

The most scorekeeper-contaminated stat on the sheet -- rink-to-rink variance is large and persistent -- so arena_hit_factor is load-bearing here, not a refinement. Role-driven and sticky, hence one of the better-projected counts.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.4722, fit on 384,861 rows (played)
- 348 trees, 34.3s, early-stop poisson 0.7476, l1 0.8505
- top features by gain: `hits_p60_std` 48.3%, `hits_p60_l20` 23.9%, `prev_hits_p60` 3.0%, `hits_p60_l10` 2.9%, `hits_l5` 1.5%, `hits_p60_l5` 1.4%

### `blocks`

Penalty-kill and defensive-zone deployment drive it, which makes it far more predictable for defencemen than for forwards.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.1202, fit on 384,861 rows (played)
- 291 trees, 28.4s, early-stop poisson 0.8458, l1 0.7005
- top features by gain: `position` 28.9%, `blocks_p60_std` 22.6%, `prev_blocks_p60` 4.0%, `blocks_p60_l20` 3.4%, `pair_known` 2.0%, `ixg_all_p60_std` 1.3%

### `assists`

Its own model rather than a twin of goals: an assist depends on line-mates' finishing, which is a different generating process from a player's own.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +0.0389, fit on 384,861 rows (played)
- 127 trees, 19.2s, early-stop poisson 0.6029, l1 0.4039
- top features by gain: `mean_pp_toi_std` 11.7%, `mean_pp_toi_l10` 11.3%, `mean_pp_toi_l20` 11.1%, `mean_pp_toi_l5` 4.0%, `assists_p60_std` 3.4%, `ev_points_p60_std` 2.7%

### `pim`

83% zeros with a lump at 2 and a thin major/misconduct tail. Tweedie was the obvious shape and was measurably worse: at variance powers 1.6/1.3/1.1 it under-predicted the holdout mean by 21.5/16.9/13.0% against Poisson's 10.7%, and lost on RMSE too. Poisson on the minutes it is. The weakest model in the stack -- barely predictable at game level.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +0.5413, fit on 384,861 rows (played)
- 275 trees, 26.5s, early-stop poisson 0.7890, l1 0.7759
- top features by gain: `pim_p60_std` 8.8%, `hits_p60_std` 6.1%, `prev_pim_p60` 4.4%, `hits_p60_l20` 2.2%, `pim_p60_l20` 1.7%, `hits_p60_l10` 1.6%

### `goals`

Chained through shots: with log(E[shots]) as the offset the trees can only learn a log-shooting-percentage correction, and the tight leaf/L2 budget is the heavy shrinkage that keeps it from fitting hot-hand noise. Deliberately NOT loosened: the elite under-projection seen on the 2025-26 holdout is league drift (shooting% has risen ~4% a season), not over-shrinkage. On the early-stop slice this setting gives +0.6% elite bias, and leaves31/min500/L2=20 and leaves63/min200/L2=5 give +1.7% and +1.2% at equal deviance and AUC -- looser is not better, just noisier. Drift is handled by drift.py.

- objective `poisson`, offset `log(shots)` with a mean-matching intercept of -2.2467, fit on 384,861 rows (played)
- 99 trees, 11.5s, early-stop poisson 0.4364, l1 0.2679
- top features by gain: `position` 31.0%, `feat_pair` 10.5%, `ixg_all_p60_std` 9.2%, `pair_known` 8.1%, `ixg_all_p60_l20` 4.8%, `mean_toi_l10` 1.7%

### `pp_point_share`

P(a given point is a power-play point), fit on scorers with each row weighted by his points. Sampling each point's strength downstream keeps PPP <= points, which an independent PPP count model would violate.

- objective `cross_entropy`, fit on 132,750 rows (scored)
- 130 trees, 7.0s, early-stop cross_entropy 0.4346
- top features by gain: `mean_pp_toi_l5` 17.6%, `mean_pp_toi_l20` 16.9%, `mean_pp_toi_l10` 16.5%, `feat_pp` 4.2%, `mean_pp_toi_std` 2.2%, `team_pp_opportunities_per_game_std` 0.7%

### `sh_point_share`

The short-handed twin of pp_point_share. Short-handed points are rare -- well under 1% of most scoring systems -- and an earlier version of this stack skipped them for that reason. That was a scoring-dependent decision baked into the models, so it is gone: a league that pays for short-handed work now gets a projection instead of a zero. Heavily regularized, because the base rate is tiny and the honest answer is usually 'almost never'.

- objective `cross_entropy`, fit on 132,750 rows (scored)
- 78 trees, 4.9s, early-stop cross_entropy 0.0884
- top features by gain: `mean_sh_toi_l10` 28.8%, `mean_sh_toi_l5` 9.0%, `mean_sh_toi_std` 6.3%, `mean_sh_toi_l20` 6.0%, `mean_ev_toi_std` 3.6%, `sh_toi_l5` 1.8%
