# Skater projection models -- variant B

Generated by `modelcards.py` from the run's own artefacts. Trained on 2023-24, 2024-25 (early stop from 2025-03-07), scored on 2025-26: 139,962 played rows of 185,577 candidates, 449 features.

These models project **stats**, not fantasy points -- no scoring system is assumed anywhere in this folder. Composite figures below appear only for scoring files that were passed to `evaluate.py --weights`.

## Accuracy against the two naive baselines

| target | mean actual | mean predicted | MAE | best baseline MAE | Spearman | CRPS | PIT deviation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `shots` | 1.545 | 1.564 | 1.014 | 1.043 | 0.430 | 0.683 | 0.008 |
| `hits` | 1.131 | 1.125 | 0.861 | 0.879 | 0.527 | 0.555 | 0.004 |
| `blocks` | 0.787 | 0.836 | 0.730 | 0.732 | 0.404 | 0.443 | 0.023 |
| `assists` | 0.290 | 0.280 | 0.398 | 0.399 | 0.267 | 0.214 | 0.005 |
| `goals` | 0.171 | 0.163 | 0.264 | 0.266 | 0.257 | 0.139 | 0.005 |
| `pim` | 0.484 | 0.422 | 0.740 | 0.773 | 0.143 | 0.423 | 0.050 |

## P(plays)

AUC 0.9906, Brier 0.0286, log loss 0.0997 against a base rate of 0.754. Reliability by decile of predicted probability:

| predicted bin | n | mean predicted | observed rate |
| --- | --- | --- | --- |
| 0.0-0.1 | 36,647 | 0.0136 | 0.014 |
| 0.1-0.2 | 3,523 | 0.1596 | 0.1513 |
| 0.2-0.3 | 645 | 0.2523 | 0.2822 |
| 0.3-0.4 | 1,085 | 0.3874 | 0.365 |
| 0.4-0.5 | 803 | 0.463 | 0.4919 |
| 0.5-0.6 | 877 | 0.5533 | 0.5074 |
| 0.6-0.7 | 3,238 | 0.6369 | 0.634 |
| 0.7-0.8 | 3,473 | 0.7528 | 0.7394 |
| 0.8-0.9 | 6,360 | 0.8416 | 0.8355 |
| 0.9-1.0 | 128,926 | 0.9894 | 0.9894 |

## Ice time

| target | mean actual | MAE | RMSE | Spearman |
| --- | --- | --- | --- | --- |
| `toi` | 992s | 108.4s | 143.6s | 0.867 |
| `ev_toi` | 837s | 110.3s | 143.3s | 0.786 |
| `pp_toi` | 85s | 46.2s | 69.6s | 0.775 |

## Under `points-league` scoring

Head-to-head weekly points league: balanced, with PP/SH point bonuses.

Weights: `goals` 4, `assists` 2.5, `ppp` 1, `shp` 1.25, `hits` 0.4, `blocks` 0.4, `shots` 0.25, `pim` 0.2.

MAE 1.858 on a mean of 2.77 points per game, Spearman 0.347, top-100 start/sit capture 0.698 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 1.898 |
| last-10 | 1.920 |

## Under `banger-league` scoring

Peripheral-heavy: hits, blocks and PIM carry most of the value.

Weights: `goals` 3, `assists` 2, `ppp` 0.5, `shp` 0.5, `hits` 1.5, `blocks` 1.5, `shots` 0.5, `pim` 1.

MAE 2.542 on a mean of 5.28 points per game, Spearman 0.395, top-100 start/sit capture 0.754 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 2.633 |
| last-10 | 2.671 |

## Under `scoring-only` scoring

Goals and assists only -- no peripherals. The hardest format to project.

Weights: `goals` 6, `assists` 4, `ppp` 2, `shp` 2.

MAE 2.701 on a mean of 2.40 points per game, Spearman 0.354, top-100 start/sit capture 0.644 (the share of each day's best-100 points the model's own best 100 actually collected).

| baseline | MAE |
| --- | --- |
| rate x TOI | 2.694 |
| last-10 | 2.703 |

## Walk-forward -- the honesty check

Refit monthly on an expanding window across the holdout season, which is how the season simulator will actually consume these models. It buys little on accuracy and helps mainly with level -- tracking the season's own rates narrows the categories that drifted (see the bias column) -- so the single fit stays the default.

| target | bias, single -> walk-forward | walk-forward MAE | single-fit MAE | PIT deviation |
| --- | --- | --- | --- | --- |
| `shots` | +1.2% -> +0.5% | 1.012 | 1.014 | 0.007 |
| `hits` | -0.6% -> -0.6% | 0.857 | 0.861 | 0.004 |
| `blocks` | +6.2% -> +4.7% | 0.726 | 0.730 | 0.017 |
| `assists` | -3.4% -> -2.3% | 0.400 | 0.398 | 0.004 |
| `goals` | -4.8% -> -3.9% | 0.265 | 0.264 | 0.004 |
| `pim` | -12.9% -> -9.9% | 0.747 | 0.740 | 0.046 |

## The live-feed bound

The same boosters scored on variant A's holdout features. `plays` AUC falls to 0.9664; the category projections barely move. That is the expected shape -- knowing tonight's lineup settles whether a player dresses, not how many shots he takes once he does.

| category | MAE | Spearman |
| --- | --- | --- |
| `shots` | 1.015 | 0.429 |
| `hits` | 0.861 | 0.526 |
| `blocks` | 0.730 | 0.402 |
| `assists` | 0.397 | 0.267 |
| `goals` | 0.263 | 0.257 |
| `pim` | 0.738 | 0.139 |

## Dispersion handed to the Monte Carlo layer

`Var = mu + theta * mu^2`, fit by maximum likelihood on the holdout. The shared game-quality variance is the Gamma multiplier's variance: **0.021**.

`Var(Y) = E[Var(Y|mu)] + Var(mu)`, so the observed column is the sum of the last two, not something the NB column should match on its own.

| category | theta | mean lambda | observed variance | NB conditional variance | Var(lambda) | gap |
| --- | --- | --- | --- | --- | --- | --- |
| `shots` | 0.0643 | 1.5637 | 2.1772 | 1.7495 | 0.4453 | -0.0177 |
| `hits` | 0.1144 | 1.1245 | 1.93 | 1.3367 | 0.5905 | 0.0027 |
| `blocks` | 0.0925 | 0.8358 | 1.0784 | 0.9209 | 0.2212 | -0.0637 |
| `assists` | 0.0001 | 0.2797 | 0.3164 | 0.2797 | 0.0251 | 0.0116 |
| `goals` | 0.0073 | 0.1632 | 0.185 | 0.1635 | 0.0131 | 0.0084 |
| `pim` | 7.4994 | 0.4216 | 1.9995 | 2.0877 | 0.0444 | -0.1327 |

## Per-model detail

### `plays`

Multiplies every other projection: a projection is worth zero if he is a healthy scratch. Fit on every lockout-knowable candidate, not just players who dressed.

- objective `binary`, fit on 318,159 rows (all)
- 128 trees, 18.1s, early-stop binary_logloss 0.1092, auc 0.9893
- top features by gain: `feat_dressed` 63.2%, `games_dressed_lookback` 8.2%, `feat_starting_goalie` 7.8%, `injured_at_lockout` 4.0%, `feat_pair` 2.2%, `feat_line` 1.8%

### `toi`

Total ice time in seconds. In-game injury truncation is unforecastable but rare (0.5% of played rows under five minutes), so plain L2 holds.

- objective `regression`, fit on 245,673 rows (played)
- 185 trees, 67.1s, early-stop l2 19152.8913, l1 105.7209
- top features by gain: `mean_toi_l5` 44.4%, `mean_toi_l10` 29.2%, `mean_toi_l20` 10.6%, `mean_toi_std` 6.9%, `feat_line` 1.1%, `team_id` 1.0%

### `ev_toi`

Even-strength seconds.

- objective `regression`, fit on 245,673 rows (played)
- 108 trees, 14.3s, early-stop l2 18686.4800, l1 105.3906
- top features by gain: `mean_ev_toi_l10` 35.4%, `mean_ev_toi_l20` 24.3%, `mean_ev_toi_std` 14.7%, `mean_ev_toi_l5` 7.9%, `mean_toi_l5` 4.1%, `feat_line` 2.2%

### `pp_toi`

Power-play seconds: a large zero mass for anyone off both units. Tweedie is the obvious shape for that and is again slightly worse -- measured on the early-stop slice, p=1.4 biases the top PP decile -2.9% against Poisson's -0.8% at the same MAE. Same lesson as PIM: Tweedie over-shrinks the tail.

- objective `poisson`, fit on 245,673 rows (played)
- 259 trees, 20.6s, early-stop poisson -290.2855, l1 43.0860
- top features by gain: `mean_pp_toi_l5` 36.9%, `mean_pp_toi_l10` 29.8%, `mean_pp_toi_l20` 7.3%, `feat_pp` 3.6%, `mean_pp_toi_std` 3.4%, `team_id` 3.0%

### `shots`

High volume and fairly stable; the sanity check for the whole offset scheme, since every other count offsets on the same predicted ice time.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.7845, fit on 245,673 rows (played)
- 203 trees, 77.7s, early-stop poisson 0.7533, l1 1.0127
- top features by gain: `iff_all_p60_std` 28.8%, `ev_shots_p60_std` 8.2%, `team_id` 6.5%, `opp_team_id` 6.4%, `icf_all_p60_std` 6.2%, `shots_p60_std` 5.0%

### `hits`

The most scorekeeper-contaminated stat on the sheet -- rink-to-rink variance is large and persistent -- so arena_hit_factor is load-bearing here, not a refinement. Role-driven and sticky, hence one of the better-projected counts.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.4973, fit on 245,673 rows (played)
- 429 trees, 28.3s, early-stop poisson 0.7380, l1 0.9110
- top features by gain: `hits_p60_std` 48.0%, `hits_p60_l20` 15.1%, `opp_team_id` 4.7%, `team_id` 4.4%, `hits_p60_l10` 3.5%, `hits_p60_l5` 1.7%

### `blocks`

Penalty-kill and defensive-zone deployment drive it, which makes it far more predictable for defencemen than for forwards.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +1.1451, fit on 245,673 rows (played)
- 174 trees, 16.9s, early-stop poisson 0.8619, l1 0.7405
- top features by gain: `position` 32.4%, `blocks_p60_std` 17.2%, `team_id` 5.8%, `opp_team_id` 5.4%, `feat_pair` 3.4%, `blocks_p60_l20` 3.1%

### `assists`

Its own model rather than a twin of goals: an assist depends on line-mates' finishing, which is a different generating process from a player's own.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +0.0350, fit on 245,673 rows (played)
- 179 trees, 16.9s, early-stop poisson 0.6011, l1 0.3921
- top features by gain: `team_id` 9.1%, `mean_pp_toi_l20` 8.6%, `opp_team_id` 8.5%, `mean_pp_toi_l10` 6.6%, `mean_pp_toi_std` 5.9%, `mean_pp_toi_l5` 3.0%

### `pim`

83% zeros with a lump at 2 and a thin major/misconduct tail. Tweedie was the obvious shape and was measurably worse: at variance powers 1.6/1.3/1.1 it under-predicted the holdout mean by 21.5/16.9/13.0% against Poisson's 10.7%, and lost on RMSE too. Poisson on the minutes it is. The weakest model in the stack -- barely predictable at game level.

- objective `poisson`, offset `log(toi)` with a mean-matching intercept of +0.5444, fit on 245,673 rows (played)
- 147 trees, 14.6s, early-stop poisson 0.7647, l1 0.7203
- top features by gain: `opp_team_id` 8.4%, `hits_p60_std` 8.4%, `team_id` 8.2%, `pim_p60_std` 7.8%, `hits_p60_l20` 5.2%, `prev_pim_p60` 2.8%

### `goals`

Chained through shots: with log(E[shots]) as the offset the trees can only learn a log-shooting-percentage correction, and the tight leaf/L2 budget is the heavy shrinkage that keeps it from fitting hot-hand noise. Deliberately NOT loosened: the elite under-projection seen on the 2025-26 holdout is league drift (shooting% has risen ~4% a season), not over-shrinkage. On the early-stop slice this setting gives +0.6% elite bias, and leaves31/min500/L2=20 and leaves63/min200/L2=5 give +1.7% and +1.2% at equal deviance and AUC -- looser is not better, just noisier. Drift is handled by drift.py.

- objective `poisson`, offset `log(shots)` with a mean-matching intercept of -2.2682, fit on 245,673 rows (played)
- 278 trees, 11.5s, early-stop poisson 0.4325, l1 0.2606
- top features by gain: `position` 17.7%, `team_id` 10.2%, `feat_pair` 9.3%, `opp_team_id` 8.3%, `ixg_all_p60_std` 4.7%, `pair_known` 4.5%

### `pp_point_share`

P(a given point is a power-play point), fit on scorers with each row weighted by his points. Sampling each point's strength downstream keeps PPP <= points, which an independent PPP count model would violate.

- objective `cross_entropy`, fit on 84,303 rows (scored)
- 85 trees, 4.5s, early-stop cross_entropy 0.4437
- top features by gain: `mean_pp_toi_l20` 21.2%, `mean_pp_toi_l10` 14.9%, `mean_pp_toi_l5` 12.2%, `team_id` 7.4%, `opp_team_id` 6.4%, `mean_pp_toi_std` 3.5%

### `sh_point_share`

The short-handed twin of pp_point_share. Short-handed points are rare -- well under 1% of most scoring systems -- and an earlier version of this stack skipped them for that reason. That was a scoring-dependent decision baked into the models, so it is gone: a league that pays for short-handed work now gets a projection instead of a zero. Heavily regularized, because the base rate is tiny and the honest answer is usually 'almost never'.

- objective `cross_entropy`, fit on 84,303 rows (scored)
- 62 trees, 3.3s, early-stop cross_entropy 0.1014
- top features by gain: `mean_sh_toi_l5` 25.8%, `team_id` 8.4%, `opp_team_id` 8.2%, `mean_sh_toi_l20` 7.6%, `mean_sh_toi_l10` 7.3%, `mean_sh_toi_std` 4.8%
